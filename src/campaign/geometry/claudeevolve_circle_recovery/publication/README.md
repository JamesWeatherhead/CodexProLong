# ClaudeEvolve circle-packing recovery audit

This packet records a negative recovery result for the deleted public
`BudEcosystem/ClaudeEvolve` repository. It contains no recovered source code,
cached webpages, paper text, coordinate tables, executable files, or dynamic
replay instructions.

The claimed score was `2.6359835671240317`. No exact 26-by-3 candidate or
program generating that score was recovered. A separately retained strict
generator and its paper coordinate table both reproduced approximately
`2.635982928558` under the frozen offline verifier. The better recovered score
was `1.6680284398162826e-7` below the required first-place gate
`2.635983095360844`; therefore no candidate was submitted.

The surviving README claim allowed gaps down to `-1e-6`, while the linked
strict paper, verifier table, and generator converged on the lower score with a
`1e-8` safety margin. This inconsistency, together with the missing headline
coordinates, makes the headline unsuitable for inference or submission.

`RECOVERY_RESULT.json` preserves the numerical result and quantified search
coverage. `EXA_PROVENANCE.json` and `PAPERCLIP_PROVENANCE.json` preserve
request-level provenance without reproducing retrieved content.
`LICENSE_PROVENANCE.md` explains why all cached third-party payloads are
excluded. The parent `PUBLICATION_MANIFEST.json` is the authoritative,
default-deny allowlist.
