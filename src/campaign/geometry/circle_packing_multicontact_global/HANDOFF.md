# Codimension-three circle-packing handoff

## Frozen conclusion

The configured finite screen found no gate-clearer.  The strict gate is
`> 2.635983095360844`; the best changed topology replays at
`2.629728811166304` and is therefore `0.006254284194540105` short.

This is a quantified negative frontier, not a global impossibility result.
It covers:

- the 500 lowest total-KKT-load release triples of the canonical basin;
- a deterministic 2,000-triple whole-spectrum sample of the canonical basin;
- a deterministic 1,000-triple whole-spectrum sample of the graph-distinct
  neutral edge-flip basin;
- non-box 3-D half-space vertices only, capped at 24 objective-ranked vertices
  per released triple in the spectrum runs.

Across those frozen jobs, 3,500 release triples produced 2,848 labeled
replacement graphs, of which 2,541 were accepted by the unchanged verifier.
All 2,541 accepted roots had independently solved strict physical
counterparts.  The best strict counterpart scores `2.6297288007960344`.

## Exact non-overlap

The program does not execute any one-contact continuation, two-contact
release, 513-direction 2-D angular sweep, weak-contact release, random field,
void relocation, or fixed-graph tolerance polish.  The preceding
codimension-two packet already solved 9,270 graph systems and recorded 5,147
unlabeled classes.  This packet begins with simultaneous release of three
active constraints and requires three new inactive activations with all three
opening coordinates strictly positive.

## Domain classification

The best changed payload uses the verifier's literal `1e-9` pair tolerance and
is classified `verifier-only`; its maximum pair overrun is still within that
published verifier rule.  The separately solved zero-overlap root is
domain-valid and scores `2.6297288007960344`.  Neither approaches the gate.

## What remains open

The receipt does not close all `C(78,3) = 76,076` release triples, more than 24
vertices per sampled release, codimension four or higher, distant contact
graphs not incident to either seed, or singular/rank-changing continua.  No
rank-deficient candidate graph was encountered in the configured runs.

## External effects

Paperclip was used read-only for line-pinned primary literature.  Arena,
GitHub, issues, comments, posts, and submissions were not mutated.

## Public replay boundary

The public packet is governed by `PUBLICATION_MANIFEST.json`.  Public commands
are repository-relative and use `public_verifier_formula.py`; neither they nor
their copied-tree tests import or execute the downloaded verifier.  The raw
run directories, superseded V1 receipt/artifacts, private full-run freeze
helper, and interpreter caches remain explicitly excluded with hashes, byte
counts, reasons, and license classifications in the manifest.

## Primary hashes

- unchanged verifier:
  `2dee3fad3cfcf2729000abda43b1812eab13fd8808e16cdb278fbae1266b37ab`
- `codim3_pivots.py`:
  `a56cf2c458758f4f5ee59c48d9f80c319738402ebabdb190fc6a340d1dea8eae`
- `freeze_receipt.py`:
  `9e8139959b2d77d6bc8581fffacbc85142e5b192fb1a8ebbab7d286529ecd06a`
- append-only `receipt_v2.json`:
  `ecc7d290d3496a30c749d2294d662736b71a681c2366ba5e938c8cf2688d98e4`
- canonical best-changed payload:
  `5c02deb6df74a0a61659542aebec72b549538d38be3f92bf3b0a50c6a688409b`
- neutral best-changed payload:
  `5922fe11b94ea32e63eb37b08b040e2a6e8507044256922f344837133f9481f1`

The `PUBLICATION_MANIFEST.json` self-hash is intentionally reported outside
the self-describing manifest after its copied-allowlist verification.
